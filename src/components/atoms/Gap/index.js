import React from 'react'

const Gap = ({width, height}) => { //untuk membuat jarak di setiap komponen yang kita inginkan
    return (
        <div style={{width, height}} />
    )
}

export default Gap;
